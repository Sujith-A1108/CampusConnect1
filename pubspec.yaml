name: campus_connect
description: "A new Flutter project for campus networking."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # Removed google_fonts and font_awesome_flutter
  # Only standard Cupertino icons remain for iOS styling support
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  # If you had a fonts section here for custom local fonts, 
  # ensure it doesn't reference deleted assets.